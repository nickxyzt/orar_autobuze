<%= render partial: 'menu/menu' %>

<% if @results.size > 0 %>
  <ul>
    <% @results.each do |line| %>
      <li><%= line %></li>
    <% end %>
  </ul>
<% else %>
  <div class="alert alert-info">Nu am gasit probleme in orarul curselor!</div>
<% end %>
