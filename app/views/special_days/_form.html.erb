<%= form_with(model: special_day) do |form| %>
  <% if special_day.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(special_day.errors.count, "error") %> prohibited this special_day from being saved:</h2>

      <ul>
        <% special_day.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :day, style: "display: block" %>
    <%= form.date_field :day %>
  </div>

  <div>
    <%= form.label :kind, style: "display: block" %>
    <%= form.select :kind_id, options_for_select(SpecialDay::KINDS, special_day.kind_id) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
