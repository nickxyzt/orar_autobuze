<%= render partial: 'menu/menu' %>

<h1>Utilitar: Obtinere orar pornind de la link</h1>
<h2>Nu uita sa modifici la rezultat "last" cu indexul statiei, si zilele cand merge (daca sunt exceptii)</h2>
<%= form_tag do %>
  Introdu linkul:
  <%= text_field_tag 'input_link', params["input_link"], size: 50 %>
  <%= submit_tag 'trimite', class: 'btn btn-primary' %>
<% end %>

<%= @obtained_schedule %>
